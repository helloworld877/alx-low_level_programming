#include <stdlib.h>
#include <time.h>
#include <stdio.h>
#include "main.h"
/**
 * _abs- gets abs of a number.
 *
 * @n: A number.
 *
 * Return:
 * True if the character is lowercase, False otherwise.
 */

int _abs(int n)
{
	if (n < 0)
	{
		n = n * -1;
	}
	return (n);
}
